<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="stylesheets/blog.css">
</head>
<body class="blog">
    <% include header %>
	<div class="containerBlog">
		<div class="blogLeft">
        <% for(var i = 0; i < data.length; i++) { %>
            <div class="article">
                <h2><%= data[i].get('title') %></h2>
                <img src=<%= data[i].get('imgSrc') %> alt= <%= data[i].get('title') %> />
                <div class="articleIntro">
                    <%= data[i].get('introduce') %>
                </div>
                <div class="read">
                    阅读全文
                </div>
            </div>
        <% } %>
		</div>
		<div class="blogRight">
			<div class="listTitle">
				热门文章
			</div>
			<ul>
			</ul>
		</div>
	</div>
    <script type="text/javascript" src="lib/jquery-2.2.2.min.js"></script>
    <script>
        $(function(){
            $("header .blog").css({
                "color": "rgb(255,255,255)",
                "backgroundColor": "rgb(81,179,200)"
            });
            $("header .blog").addClass("checkBlog");
            $("header .blog").removeClass("blog");
        });
    </script>
</body>
</html>